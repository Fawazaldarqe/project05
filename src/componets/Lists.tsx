import { ReactElement } from 'react';

import { Navbar } from './Navbar';

interface ListsProps {
	children: ReactElement;
}
export function Lists({ children }: ListsProps) {
	return (
		<>
			

		</>
	);
}